# Preliminaries

# 1 编译流程

1. **源代码阶段**：程序员编写源代码文件（如 **`.cpp`** 或 **`.h`** 文件），其中包含了各种声明、定义、宏定义等。
2. **预处理阶段**：在这个阶段，预处理器首先对源代码进行处理，主要包括以下几个步骤：
    
    a. **移除注释**：预处理器会移除源代码中的注释，包括单行注释 **`//`** 和多行注释 **`/* */`**。
    
    b. **宏替换**：预处理器会对源代码中出现的宏进行替换。宏定义以 **`#define`** 开头，预处理器会找到源代码中使用该宏的地方，并将其替换为宏定义中指定的文本。
    
    c. **文件包含**：预处理器会处理 **`#include`** 指令，将指定的文件内容插入到当前源文件中。这样可以将头文件的内容包含到源文件中，使得源文件可以访问头文件中定义的声明和定义。
    
    d. **条件编译**：预处理器会根据 **`#if`**、**`#ifdef`**、**`#ifndef`**、**`#else`**、**`#elif`** 和 **`#endif`** 等条件编译指令来决定哪些代码应该被编译，哪些代码应该被忽略。
    
    e. **其他预处理指令**：预处理器还支持一些其他的预处理指令，如 **`#undef`**（取消宏定义）、**`#pragma`**（编译器指令）等。
    
3. **生成预处理后的文件**：预处理器处理完源代码后，将生成预处理后的中间文件，通常以 **`.i`** 或者 **`.ii`** 作为扩展名。
4. **编译阶段**：编译器会将预处理后的文件作为输入，进行词法分析、语法分析、语义分析等操作，生成汇编代码。
5. **汇编阶段**：汇编器将编译器生成的汇编代码翻译成机器码。
6. **链接阶段**：链接器将编译器生成的目标文件和库文件链接在一起，生成可执行文件。

# 2 声明和定义

理清关于在常量、函数等在头文件中声明、在源文件中定义等等问题。