# C++ 核心语法

# 1 常量、变量、基本数据类型、运算符

## 1.1 常量

1. **const 修饰的常量**：使用 **`const`** 关键字声明的常量，在程序执行过程中不可更改。可以用于任何数据类型，包括基本数据类型、自定义数据类型以及指针。**一旦声明并初始化，其值就不能再被修改。**
    
    ```cpp
    const int MAX_VALUE = 100;
    const float PI = 3.14159;
    const char* GREETING = "Hello";
    
    // const 修饰的常量也可以用于指针
    const int* ptr = &MAX_VALUE;  // 指向整型常量的指针，指针指向的值不能被修改
    ```
    
2. **宏定义常量**：**`#define`** 是 C++ 中用来创建宏（macro）的预处理指令。它的作用是在编译之前将代码中出现的宏名称替换为相应的文本。宏定义可以用于定义常量、简单的函数以及代码片段的重用。
    
    ```cpp
    #define MAX_VALUE 100
    #define PI 3.14159
    ```
    
    然而，宏定义存在一些缺点，如可能导致代码可读性降低、调试困难等。因此，在实际编程中，建议使用常量、内联函数、模板等更安全、可读性更高的方式替代。
    
3. **枚举常量**：枚举常量是一种有限集合中的命名常量。通过 **`enum`** 关键字定义，枚举常量的值默认从 0 开始递增，每个枚举常量都会比前一个大1，但也可以手动指定值。
    
    ```cpp
    enum Weekday {
        Monday,     // 默认值为0
        Tuesday,    // 默认值为1
        Wednesday,  // 默认值为2
        Thursday,   // 默认值为3
        Friday,     // 默认值为4
        Saturday,   // 默认值为5
        Sunday      // 默认值为6
    };
    
    enum Weekday {
        Monday = 1,     // 指定值为1
        Tuesday = 2,    // 指定值为2
        Wednesday = 3,  // 指定值为3
        // 其他枚举常量依次递增
    };
    
    Weekday day1 = Monday;
    ```
    

## 1.2 变量

静态变量、作用域等概念。

## 1.3 基本数据类型

1. **整数类型**：
    - **`int`**：整数类型，通常为32位（取决于平台）。
    - **`short`**：短整数类型，通常为16位。
    - **`long`**：长整数类型，通常为32位（在某些系统中可能为64位）。
    - **`long long`**：长长整数类型，通常为64位。
    - **`signed`**和**`unsigned`**修饰符用于声明有符号和无符号整数。
2. **浮点数类型**：
    - **`float`**：单精度浮点数，通常为32位，有效位数6-7。
    - **`double`**：双精度浮点数，通常为64位，有效位数15-16。
    - **`long double`**：长精度的浮点数，有效位数18-19。
3. **字符类型**：
    - **`char`**：字符类型，通常为8位。
    - **`char16_t`**：16位字符类型。
    - **`char32_t`**：32位字符类型。
    - **`wchar_t`**：宽字符类型，适用于宽字符编码。
    
    通过ascii码表转义，65:A，97:a。
    
4. **布尔类型**：
    - **`bool`**：布尔类型，存储true或false，本质是1和0。
5. **空类型**：
    - **`void`**：表示无类型或无返回值。
    

除了这些基本类型外，C++还支持一些复合数据类型，如数组、结构体、指针、引用、联合体和类等。

其中包括常用的**字符串**，通常用字符数组（char array）或者字符串类（string class）来表示。

- **C风格字符串**：在C中，字符串通常表示为以null结尾的字符数组。在C++中，仍然可以使用这种方式来处理字符串，但是要小心处理null终止符以及数组边界的问题。
    
    ```cpp
    char str1[] = "Hello"; // 字符串以null结尾
    char *str2 = "Hello"; // 指针形式
    ```
    
- **C++标准库中的字符串类**：C++提供了**`std::string`**类来方便地处理字符串。这个类提供了许多成员函数和操作符来进行字符串的操作，包括连接、比较、查找、替换等。使用**`std::string`**类可以避免C风格字符串的许多问题，如内存管理和边界检查等。
    
    ```cpp
    #include <string>
    #include <iostream>
    
    int main() {
        std::string str = "Hello";
        std::cout << str << std::endl; // 输出：Hello
        return 0;
    }
    ```
    

## 1.4 运算符

1. **算术运算符**：用于执行基本的数学运算，如：**`+`** 加法、**`-`** 减法、 **`*`** 乘法、 **`/`** 除法、 **`%`** 取模。
2. **赋值运算符**：用于将值赋给变量。如：**`=`** 赋值、**`+=`** 加法赋值、**`=`** 减法赋值、**`=`** 乘法赋值、**`/=`** 除法赋值、**`%=`** 取模赋值。
3. **关系运算符**：用于比较两个值的关系，返回布尔值（true或false）。如：**`==`** 等于、**`!=`** 不等于、 **`<`** 小于，等等。
4. **逻辑运算符**：用于进行逻辑运算，操作布尔值。有：
    - **`&&`** 逻辑与（AND）
    - **`||`** 逻辑或（OR）
    - **`!`** 逻辑非（NOT）
5. **位运算符**：用于对二进制位进行操作。
    - **`&`** 按位与
    - **`|`** 按位或
    - **`^`** 按位异或
    - **`~`** 按位取反
    - **`<<`** 左移
    - **`>>`** 右移
    
    举例来说，假设有两个无符号整数 **`a = 5`** 和 **`b = 3`**，它们的二进制表示分别为 **`a = 101`**，**`b = 011`**。
    
    - **`a & b`** 的结果是 **`001`**，即 **`1 & 1 = 1`**，其他位都是0。
    - **`a | b`** 的结果是 **`111`**，即 **`1 | 1 = 1`**，其他位都是1。
    - **`a ^ b`** 的结果是 **`110`**，即 **`1 ^ 1 = 0`**，其他位都是1。
    - **`~a`** 的结果是 **`11111111111111111111111111111010`**，即各位取反。
    - **`a << 1`** 的结果是 **`1010`**，即将 **`101`** 左移1位并在右侧填充0，相当于将5乘以2。
    - **`b >> 1`** 的结果是 **`001`**，即将 **`011`** 右移1位并在左侧填充0，相当于将3除以2。
6. **三元运算符：也称为条件运算符**，是C++中唯一的具有三个操作数的运算符。它的一般形式是：
    
    ```cpp
    condition ? expression1 : expression2
    ```
    
    它的行为是根据条件 **`condition`** 的结果选择性地执行 **`expression1`** 或 **`expression2`** 中的一个，并返回相应的结果。如果 **`condition`** 为真（非零），则整个表达式的值为 **`expression1`** 的值，否则为 **`expression2`** 。
    
7. **`sizeof`运算符**：可以用 **`sizeof`** 统计数据类型所占内存大小。**`sizeof`** 是 C++ 中的一个运算符，用于获取给定类型或变量的大小（以字节为单位）。
    
    ```cpp
    #include <iostream>
    using namespace std;
    
    int main()
    {
        cout << "Size of int: " << sizeof(int) << " bytes" << endl;       // Size of int: 4 bytes
        cout << "Size of float: " << sizeof(float) << " bytes" << endl;   // Size of float: 4 bytes
        cout << "Size of char: " << sizeof(char) << " byte" << endl;      // Size of char: 1 byte
        cout << "Size of double: " << sizeof(double) << " bytes" << endl; // Size of double: 8 bytes
    
        int arr[10];
        cout << "Size of arr: " << sizeof(arr) << " bytes" << endl; // Size of arr: 40 bytes
    
        return 0;
    }
    ```
    

# 2 基本控制结构

C++ 的基本控制结构包括条件语句（if-else、 switch）、循环语句（for、while、do-while）以及跳转语句（break、continue、return）。

switch 语句允许根据一个表达式的值执行不同的代码块。它与一系列 if-else 语句相比，更适合用于多个条件值的情况。

```cpp
switch (expression) {
    case value1:
        // 当 expression 等于 value1 时执行这里的代码
        break;
    case value2:
        // 当 expression 等于 value2 时执行这里的代码
        break;
    // 可以有更多的 case 分支
    default:
        // 如果 expression 不等于任何一个 case 的值，则执行这里的代码
        break;
}
// Note: break不要漏了，不然会从第一次匹配的case开始往下贯穿执行, 直到遇到break语句或switch语句的结束
```

除此之外，常用的还有try语句块，用于异常处理。

# 3 数组、结构体

前面提到除了基本数据类型外，C++还支持一些复合数据类型，这里介绍两个常见的：数组和结构体

## 3.1 数组

C++中的数组是一种用于存储相同类型的元素序列的数据结构。数组提供了一种便捷的方法来存储和访问多个相同类型的数据。

1. **声明数组**
    
    在C++中声明数组的基本语法如下：
    
    ```cpp
    type arrayName[arraySize];
    ```
    
    其中：
    
    - **`type`** 是数组中元素的类型，可以是任何基本类型（如整数、浮点数等）或自定义类型（如结构体、类等）。
    - **`arrayName`** 是数组的名称，你可以根据需要选择合适的名称。
    - **`arraySize`** 是数组的大小，即数组中元素的数量。
    
    比如：
    
    ```cpp
    int myArray[5];  // 声明一个包含5个整数的数组
    int myArray[5] = {1, 2, 3, 4, 5};  // 声明一个包含5个整数的数组，并初始化
    ```
    
2. **数组的特性**
    - **连续存储**：数组中的元素在内存中是连续存储的，这使得数组能够高效地进行访问。
    - **固定大小**：一旦声明了数组并指定了大小，其大小在整个程序生命周期内保持不变。
    - **静态分配**：数组的大小在编译时就确定了，因此称为静态分配。这也意味着数组大小不能动态地根据程序运行时的需要进行更改。
    - **快速访问**：由于数组元素在内存中的连续存储，因此可以通过下标快速访问任何元素。
    - 数组不允许拷贝和赋值：不能直接将数组拷贝给其他数组作为初始值，也不能直接用数组为其他数组赋值：
        
        ```cpp
        int a[] = {1,2,3};
        int a2[] = a; // 错误，不能直接将数组拷贝给其他数组作为初始值
        a2 = a; // 错误，不能直接用数组为其他数组赋值
        ```
        

## 3.2 结构体

在C++中，结构体（struct）是一种用户自定义的数据类型，用于组合不同类型的数据成员。结构体的定义通常用于表示一个具有多个相关属性的实体。

示例：

```cpp
#include <iostream>
#include <string>

// 定义一个结构体
struct Person {
    std::string name;
    int age;
    float height;
};

int main() {
    // 使用结构体 (struct可不加，但加上可读性更好)
    struct Person person1;
    person1.name = "Alice";
    person1.age = 30;
    person1.height = 1.75;

    std::cout << "姓名：" << person1.name << std::endl;
    std::cout << "年龄：" << person1.age << std::endl;
    std::cout << "身高：" << person1.height << "米" << std::endl;

    return 0;
}
```

1. **结构体的成员**
    
    结构体中的成员可以包括各种数据类型，如整数、浮点数、字符、数组、指针等。成员可以通过**`.`**操作符进行访问和操作
    
2. **结构体的初始化**
    
    结构体可以在声明时初始化，也可以在创建结构体变量后逐个初始化。以下是两种初始化方式的示例：
    
    ```cpp
    // 在声明时初始化
    struct Person person2 = {"Bob", 25, 1.80};
    
    // 创建结构体变量后逐个初始化
    struct Person person3;
    person3.name = "Charlie";
    person3.age = 35;
    person3.height = 1.70;
    ```
    
3. **结构体排序**
    
    使用标准模板库（STL）的排序函数可以更简洁地对结构体进行排序。STL提供了**`std::sort`**函数，它能够对容器或者数组进行排序，我们只需要提供一个比较函数来指定排序的方式。
    
    ```cpp
    #include <iostream>
    #include <algorithm>
    #include <string>
    #include <vector>
    
    struct Person {
        std::string name;
        int age;
        float height;
    };
    
    // 比较函数，用于指定按照年龄进行排序
    bool compareByAge(const Person &a, const Person &b) {
        return a.age < b.age;
    }
    
    int main() {
        std::vector<Person> people = {
            {"Alice", 30, 1.65},
            {"Bob", 25, 1.80},
            {"Charlie", 35, 1.70},
            {"David", 28, 1.85},
            {"Emma", 22, 1.75}
        };
    
        // 使用 std::sort 对结构体数组进行排序，使用 compareByAge 函数指定按照年龄排序
        std::sort(people.begin(), people.end(), compareByAge);
    
        // 输出排序后的结果
        std::cout << "按照年龄排序：" << std::endl;
        for (const auto &person : people) {
            std::cout << "姓名：" << person.name << ", 年龄：" << person.age << ", 身高：" << person.height << "米" << std::endl;
        }
    
        return 0;
    }
    ```
    
    C++11引入了lambda表达式，允许我们在需要比较函数时使用匿名函数，这样可以更加简洁地进行排序。以下是使用lambda表达式对结构体数组按照年龄进行排序的示例：
    
    ```cpp
      // 使用 lambda 表达式指定按照年龄排序
        std::sort(people.begin(), people.end(), [](const Person &a, const Person &b) {
            return a.age < b.age;
        });
    ```
    

# 4 引用和指针

一个简单变量的声明语句格式是**`基本数据类型 声明符`**，一般声明符就是变量名（比如`int a`中的`a`），此时变量的类型就是声明的基本数据类型`int`。

然而还有更复杂的声明符，它基于基本数据类型得到更复杂的类型，并把它指定给变量，比如引用和指针。

## 4.1 引用

C++中的引用（References）是用于**为变量提供别名**的特性，它与被引用的变量绑定，并与该变量共享相同的内存地址，而不是新建一个对象并把原变量的值拷贝过来（严格来说，引用甚至都不是对象）。

- **引用的定义格式**：通常将声明符写成`&a`的形式，即在变量名前加&。
    
    ```cpp
    int original = 10;
    int &ref = original; // ref是original的引用
    ref = 20; // 现在original的值也变为20
    ```
    
- 引用即别名，它只是为已存对象所起的另外一个名字。**因此引用在声明时必须初始化**。
    
    ```cpp
    int &ref; // 错误：引用必须在声明时初始化
    int &ref = 10; // 错误：引用的初始值必须是一个对象，而不能是临时的字面值。
    ```
    
- 定义了一个引用后，对其进行的所有操作都是在与之绑定的对象上进行的，即一直绑定在一起。**所以引用无法更改绑定到其他对象。**
    
    ```cpp
    int a = 5;
    int b = 10;
    int &ref = a;
    ref = b; // 这并不是重新绑定ref到b，而是将a的值改为10
    ```
    
- **对常量的引用（reference to const）**
    
    当我们定义了一个const对象时，我们也可以创建它的引用，即对常量的引用。不可以通过对常量的引用修改其绑定对象的值。
    
    这里的语法可以从右往左解读，`&r1` 表示r1是一个引用，前面的`const int`表示`r1`指向的是一个int型常量：
    
    ```cpp
    const int ci = 1;
    const int &r1 = ci;
    int &r2 = ci;  // 错误语法，试图让一个对非常量的引用去指向一个常量对象（但是反过来可以，即让一个对常量的引用指向一个非常量对象）
    r1 = 0; // 错误语法，既然是对常量的引用，自然不能改变指向的常量对象的值。
    ```
    
    允许将对常量的引用绑定到一个非常量对象、字面值，甚至是一般表达式。
    
    ```cpp
    int i = 1;
    const int &r1 = i;                                      // 对常量的引用可以绑定到变量
    const int &r2 = 2;                                      // 对常量的引用可以绑定到字面值
    const int &r3 = r1 * 3;                                 // 对常量的引用可以绑定到一般表达式
    std::cout << r1 << " " << r2 << " " << r3 << std::endl; // output: 1 2 3
    // r1 = 0;  // 错误，不能通过对常量的引用修改绑定的对象，即使对象本身是非常量的
    ```
    
    所以，有趣的是，普通的引用无法绑定到临时量，但是对常量的引用却可以。这是由于临时量无法被修改，而普通引用是可以修改指向对象的，所以普通引用指向临时量会导致修改不可修改对象的风险，而对常量的引用则不会，反而会在比如传递函数参数等情况时避免不必要的拷贝而提高性能。常见的用法有：
    
    ```cpp
    void print(const int &x) {
        std::cout << x << std::endl;
    }
    int main() {
        print(42);  // 临时对象 42 被对常量的引用接收
        return 0;
    }
    ```
    

## 4.2 指针

指针（Point）是指向另外一种另外一种类型的复合类型，本质上来说，指针是一个存放某个对象地址的变量。

类似引用，指针也实现了对其他对象的间接访问，与引用相比主要有如下两个区别：

1. 指针本身就是一个对象，而引用只是别的对象的别名，指针可以被赋值或者拷贝，所以可以更改指向对象。
2. 指针在定义时无须赋初值，和其他内置类型一样，如果没有被初始化，就会有用一个不确定的值。
- **指针的定义格式与取地址符**
    
    定义一个指针时需要将声明符写成`*p`的形式，其中`p`是变量名，`*` 表示p是一个指针型变量；取地址符为`&`，由于指针保存另一个对象的地址，所以在定义或者为指针赋值时常常会用到取地址符。
    
    ```cpp
    int i = 1;
    int *p1 = &i;                  // 取i的地址 为指针p1初始化
    int *p2 = p1;                  // 取指针p1的值 为指针p2初始化
    std::cout << *p1 << std::endl; // 输出指针p1的值 output: 1
    std::cout << *p2 << std::endl; // 输出指针p2的值 output: 1
    
    // 定义一个空指针（不指向任何对象）的三种方法
    int *np1 = 0;
    int *np2 = nullptr;  // 等价 int *np2 = 0
    int *p = NULL;  // 需要 #include cstdlib
    ```
    
- **解引用符**
    
    有意义的指针会指向一个对象，而当我们想要访问被指向对象时，需要用到解引用符`*` 。
    
    ```cpp
    // 通过解引用符号*，得到的*p1即p1指向的i对象，此时无论是输出i，还是*p1，*p2，结果都会是0
    *p1 = 0;
    std::cout << *p1 << std::endl; // 输出指针p1的值 output: 0
    std::cout << *p2 << std::endl; // 输出指针p2的值 output: 0
    std::cout << i << std::endl;   // 输出i的值 output: 0
    ```
    

注：`*`和`&`在声明语句中用于组成符合类型的，而在正常表达式里则表示用于解引用和取地址的运算符，其含义完全不同，所以完全可以在不同场景下把它们当作不同的符号。

- **指向常量的指针（point to const）**
    
    和对常量的引用类似，由于常量是一个对象，当然可以定义指向常量的指针，但是无法通过该指针改变其所指对象的值。
    
    ```cpp
    const double pi = 3.14;
    const double *cptr = &pi;
    *cptr = 3.14159; // error: 对常量的指针不能用于修改其所指向的对象
    double *ptr = &pi; // error: 不能用一个普通指针指向一个常量对象
    ```
    
    和对常量的引用类似，可以令一个指向常量的指针指向一个非常量对象（这里可能有点奇怪，但是一个指向常量的指针，其实只是这个指针自以为指向的是常量，所以无法通过该指针改变其指向对象的值，但是他实际指向的是不是常量是不作规定的）。
    
- **常量指针（const pointer）**
    
    指针和引用有一点不同的是，指针本身就是一个对象，所以可以把指针本身定义为常量。和一般的常量一样，**常量指针在初始化完成后，就不能改变其值了，即不能改变其指向了**。
    
    ```cpp
    double pi = 3.14;
    double pi_detail = 3.1415;
    
    double *const ptr1 = &pi;  // 常量指针
    const double *ptr2 = &pi;  // 指向常量的指针
    
    ptr1 = &pi_detail; // 语法错误：ptr1是常量指针，不能改变ptr1的值
    *ptr1 = 3.1415;    // 可以改变ptr1所指对象的值
    
    ptr2 = &pi_detail; // 可以改变ptr2自身的值，即ptr2可以指向其他对象
    *ptr2 = 3.1415;    // 语法错误：cptr2是指向常量的指针，不能通过cptr2改变其所指对象的值
    ```
    
- **指针和数组**
    
    数组有一个特性：在大多数表达式中，编译器会自动将数组名字替换为一个指向数组首元素的指针。
    
    这里还需要注意一下指向指定大小的数组的指针在定义时需要括号，否则就变成了定义一个包含指针对象的数组。
    
    ```cpp
    int main(int argc, char *argv[])
    {
        std::string numbers[] = {"one", "two", "three"};
        std::string *p1 = numbers; // same as &numbers[0]，p1是指向string类型的指针，而数组名numbers隐式转换为指向数组首元素（string类型）的指针
        std::string *p2 = &numbers[0];
        std::cout << *p1 << std::endl; // one
        std::cout << *p2 << std::endl; // one
    
        std::string(*p3)[3] = &numbers;     //  注意，如果没有括号，p3会被解析为包含3个指向string的指针的数组，而不是指向包含3个string的数组的指针
        std::cout << (*p3)[0] << std::endl; // one
        return 0;
    }
    ```
    

## 4.3 引用+指针

试想，既然指针本身是个变量，而引用可以为变量创建别名，那么自然就会有指向指针的引用。但是在语法上，需要从右往左解读，比如`int *&r = p;` 中`&r`表示了r是一个引用，所以`r`是指向`p`的引用，而由于`p`是一个int型的指针，所以在前面需要加上 `int *` 。

```cpp
int i = 1;
int *p = &i;                 // p是指向i的指针
int *&r = p;                 // r是指向指针p的引用，所以r本质上还是一个引用
*r = 0;                      // 作为p的引用，通过r可以改变i的值
std::cout << i << std::endl; // output: 0
```

反过来，我们就会发现，引用本身并不是一个对象，所以就不能定义指向引用的指针了。

# 5  函数

## 5.1 局部对象和局部静态对象

- **局部对象**
    
    一般来说，函数的形参和内部定义的对象都是**局部对象**，它们的生命周期仅限于函数的执行过程。也就是说，局部对象在函数调用时被创建，在函数返回时被销毁。
    
- **局部静态对象**
    
    而在某些时候，有必要令局部变量的生命周期贯穿函数调用及之后的时间，则可以将局部变量定义成`static`类型从而得到局部静态对象。局部静态对象的生命周期跨越整个程序的执行过程，它们会在程序第一次经过对象定义时初始化一次，并在程序结束时销毁。
    
    ```cpp
    void count_calls()
    {
        static int ctr = 0; // 局部静态对象，记录该函数调用次数
        ctr++;              // 修改局部静态对象的值
        std::cout << ctr << std::endl;
    }
    int main()
    {
        for (int i = 0; i != 10; ++i)
            count_calls(); // 依次输出1-10
    
        return 0;
    }
    ```
    

## 5.2 参数传递

**形参**是函数定义中指定的变量，用于接收传递给函数的值或对象。它们出现在函数的参数列表中，并在函数体内作为局部变量使用。**实参**是函数调用时实际传入的值或对象，它们与形参对应。

每次调用函数时，函数内部都会重新创建形参，并用传入的实参进行初始化。

- **值传递和指针形参**
    
    **值传递**意味着将实参的副本传递给函数，用以初始化形参对象。在函数内部对形参的任何修改不会影响外部的实参原始值。
    
    如果形参定义为一个指针，即**指针形参**，则传入的也只是实参指针的拷贝，虽然可以在函数内部通过这个指针修改其指向的外部变量，但无法改变实参指针本身值。
    
    ```cpp
    void try_reset_p(int *ip)
    {
        *ip = 0; // 可以通过指针ip修改指向的外部值
        ip = 0;  // 但是不能修改指针ip本身，只是修改了ip的拷贝
    }
    void try_reset_i(int i)
    {
        i = 0; // 不能通过形参值i修改外部的实参i
    }
    int main()
    {
        int i = 10;
        int *ip = &i;
        try_reset_p(ip);                          // ip是指针，传递的是指针的拷贝，所以ip指向的i会被修改，但是ip本身不会被修改
        std::cout << "ip: " << ip << std::endl;   // ip: 0x16fdff328，ip本身没有被修改，还是指向i的地址
        std::cout << "*ip: " << *ip << std::endl; // *ip: 0，ip指向的i被修改为0
        std::cout << "i: " << i << std::endl;     // i: 0，ip指向的i被修改为0
    
        i = 10;
        try_reset_i(i);                       // i是值，传递的是i的拷贝，所以i不会被修改
        std::cout << "i: " << i << std::endl; // i: 10，i没有被修改
        return 0;
    }
    ```
    
- **引用传递**
    
    **引用传递**将参数的引用传递给函数，这意味着函数内部可以直接修改外部变量的值。引用传递通常用于避免不必要的复制，并且可以修改外部数据。
    
    ```cpp
    void try_reset_i(int &i)
    {
        // i是引用形参，这里的i就是main函数中的对应实参的别名
        i = 0;
    }
    int main()
    {
        int i = 10;
        try_reset_i(i);
        std::cout << i << std::endl; // 0
        return 0;
    }
    ```
    
    注意，虽然可以通过指针形参访问函数外部的对象，但是在C++中，建议使用引用类型形参来代替指针形参
    
    使用引用传递，可以避免拷贝，使得操作更加搞笑，比如下方的常见例子：string对象可能比较长，所以尽量避免拷贝他们，可以通过引用传递来避免不必要的拷贝操作。而比较string长度无需改变string对象的值，所以把形参定义为对常量的引用。
    
    ```cpp
    bool isShorter(const std::string &s1, const std::string &s2)
    {
        return s1.size() < s2.size();
    }
    ```
    
    实际上，在定义函数不会改变的形参时，需要尽量定义为常量引用，这样不会在调用时产生误导，而且可以扩大函数能接受的实参类型（比如普通引用形参无法接受const对象、字面值等）
    
- 数组形参
    
    先明确数组的两个特殊性质对其作为函数参数的影响：
    
    - **不允许拷贝数组**，所以无法通过值传递来使用数组参数。
    - **使用数组时通常会将其转化为指针**，所以为函数传递数组时实际上传递的是指向数组首元素的指针。
        
        所以如下几个形参定义的函数是等价的，即**数组作为函数参数时，会自动退化为指针**
        
        ```cpp
        void print_a(const int *value)
        {
            std::cout << *value << std::endl;
        }
        void print_b(const int value[])
        {
            std::cout << *value << std::endl;
        }
        void print_c(const int value[5])  // 退化为指针，这里的5只能表示我们期望数组有多少元素，实际上不一定
        {
            std::cout << *value << std::endl;
        }
        int main()
        {
            int i = 2, j[2] = {3, 4};
            print_a(&i); // 2
            print_b(j);  // 3
            print_c(j);  // 3
            return 0;
        }
        ```
        
- 关于main的命令行选项
    
    在 C++ 中，main 函数可以接收命令行参数，通过 argc 和 argv 进行处理：
    
    ```cpp
    #include <iostream>
    
    int main(int argc, char* argv[]) {
        std::cout << "Argument count: " << argc << std::endl;
    
        for (int i = 0; i < argc; ++i) {
            std::cout << "argv[" << i << "]: " << argv[i] << std::endl;
        }
    
        return 0;
    }
    
    // 终端运行： ./program arg1 arg2 arg3 （假设program是构建到的可执行文件）
    // 得到
    // Argument count: 4
    // argv[0]: ./hello
    // argv[1]: arg1
    // argv[2]: arg2
    // argv[3]: arg3
    ```
    
    - argc（**argument count**）：表示命令行参数的个数，包括程序本身的名称。
    - argv（**argument vector**）：是一个字符串数组，存储每个参数的值，argv[0] 通常是程序的名称，所以实际参数从下标1开始。
- 可变形参数

## 5.3 函数返回

## 5.4 函数重载

如果同一作用域内的几个**函数名字相同**，但是**形参列表不同**，我们称之为重载（overloaded）函数。

定义了一组重载函数后，需要以合理的实参调用它们，编译器会将实参与重载集合中每一个函数的形参做比较，然后决定调用哪一个函数。如果无匹配或者有多个可以匹配，则会发生错误。

## 5.5 函数指针

函数指针指向的是函数而非对象，函数指针指向某种特定类型的函数，而函数的类型由其返回类型和形参类型共同决定，与函数名无关。